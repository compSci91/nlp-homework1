NOTE TO THE GRADER:

The program has already been compiled for your convenience.
In order to run, navigate to the build/classes/main folder.
There, you should see four classes.
Then, execute the command java Main [directory/to/files]

example: java Main /data/dev/

Results:

When executing the code on /data/dev, it successfully finds all phone numbers
and emails. If an email is present more than once, it will be displayed twice. 
